<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.it.ez.addrbook.model.AddrBookDAO">
	<insert id="insertBook" parameterType="AddrBookVO">
		<selectKey resultType="int" keyProperty="bookNo" order="BEFORE">
			select addr_seq.nextval from dual
		</selectKey>
		insert into addrbook(bookNo, deptName, posName, name, 
						corp_phone,  address, addressDetail, hp, email, memo )
		values(#{bookNo},  #{deptName, jdbcType=VARCHAR}, #{posName, jdbcType=VARCHAR}, #{name}, 
			#{corp_phone, jdbcType=VARCHAR},#{address, jdbcType=VARCHAR},#{addressDetail, jdbcType=VARCHAR}, #{hp, jdbcType=VARCHAR}, #{email, jdbcType=VARCHAR},#{memo, jdbcType=VARCHAR})
	</insert>
	
	<select id="selectAll" parameterType="addrBookVo" resultType="addrBookVO">
		select *
		from
		(
			select rownum as RNUM,A.*
			from 
			(
				select * from addrbook
				order by bookNo desc
			)A
		)
		<![CDATA[
		where RNUM > #{firstRecordIndex}
		and RNUM <= #{firstRecordIndex} + #{recordCountPerPage}
		]]>
	</select>
	
	<select id ="selectTotalRecord" parameterType="addrBookVo"
		resultType="int">
		select count(*) from addrbook
	</select>
	
	

</mapper>









