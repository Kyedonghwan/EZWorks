<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.it.ez.addrbook.model.CoAddrBookDAO">
	<select id="selectAll" parameterType="coEmpVo" resultType="coEmpVo">
		select *
		from
		(
			select rownum as RNUM,A.*
			from 
			(
				select * from coaddr_view
				order by fk_emp_no desc
			)A
		)
		<![CDATA[
		where RNUM > #{firstRecordIndex}
		and RNUM <= #{firstRecordIndex} + #{recordCountPerPage}
		]]>
	</select>
	
	<select id ="selectTotalRecord" parameterType="coEmpVo"
		resultType="int">
		select count(*) from coaddr_view
	</select>
	




</mapper>