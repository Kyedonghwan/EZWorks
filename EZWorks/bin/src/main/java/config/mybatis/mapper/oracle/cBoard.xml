<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.it.ez.communityBoard.model.C_boardDAO">	
	<insert id="insertC_board" parameterType="C_boardVO">
		<selectKey keyProperty="boardNo" resultType="int" order="BEFORE">
			select c_board_seq.nextval from dual
		</selectKey>
		insert into c_board(boardNo, boardName, boardMaster, intro, publicSet)
		values(#{boardNo}, #{boardName}, #{boardMaster}, #{intro}, #{publicSet})
	</insert>
	
	<insert id="insertBoardContent" parameterType="C_boardContentVO">
		<selectKey keyProperty="contentNo" resultType="int" order="BEFORE">
			select c_boardContent_seq.nextval from dual
		</selectKey>
		insert into c_boardContent(contentNo, boardNo, name, title, text, 
			groupNo, images, fileName, fileSize, originalFileName)
		values(#{contentNo}, #{boardNo} , #{name}, #{title}, #{text}, 
			#{contentNo}, #{images, jdbcType=VARCHAR}, #{fileName, jdbcType=VARCHAR}, #{fileSize}, #{originalFileName, jdbcType=VARCHAR})
	</insert>
	
	<select id="selectC_board" parameterType="int" resultType="C_boardVO">
		select * from c_board
		where communityNo=#{communityNo}
		order by boardNo
	</select>
	
	<select id="selectC_boardContent" resultType="C_boardContentVO">
		select * from c_boardContent
		order by contentNo desc
	</select>
	
</mapper>









